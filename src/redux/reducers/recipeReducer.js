const INITIAL_VALUE = {
  recipe: {},
  isFetching: false,
  error: null,
  inProgress: true,
};

const recipe = (state = INITIAL_VALUE, action) => {
  switch (action.type) {
  case value:
    log
    break;

  default:
    break;
  }
}